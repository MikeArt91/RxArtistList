<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mikeart.rxartistlist.presentation">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <application
      android:allowBackup="true"
      android:name="com.mikeart.rxartistlist.presentation.AndroidApplication"
      android:icon="@drawable/ic_launcher"
      android:label="@string/app_name"
      android:theme="@style/AppTheme">

    <activity
        android:name="com.mikeart.rxartistlist.presentation.view.activity.ArtistListActivity"
        android:theme="@style/AppTheme.NoActionBar">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
        android:name="com.mikeart.rxartistlist.presentation.view.activity.ArtistDetailsActivity"
        android:theme="@style/AppTheme.NoActionBar"
        android:label="@string/activity_title_artist_details">
    </activity>
  </application>

</manifest>
